/* import { TestBed } from '@angular/core/testing';

import { ScrollFunctionService } from './scroll-function.service';

describe('ScrollFunctionService', () => {
  let service: ScrollFunctionService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(ScrollFunctionService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
